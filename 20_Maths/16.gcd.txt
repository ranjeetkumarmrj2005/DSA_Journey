



class Solution {
public:
    int gcd(int a, int b) {
        // Base case: if b becomes 0, a is the GCD
        if (b == 0) return a;

        // Recursive step: gcd(b, remainder of a/b)
        return gcd(b, a % b);
    }
};