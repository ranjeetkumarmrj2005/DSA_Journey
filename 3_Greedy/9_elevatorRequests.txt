class Solution {
public:
    int elevatorRequests(int n, vector<int>& requests) {
        int ans=requests[0];
        int p=requests.size();
        for(int i=1;i<p;i++){
            ans=ans+abs(requests[i]-requests[i-1]);
        }
        return ans;
    }
};