

class Solution {
  public:

    string firstAlphabet(string &s) {
        int n=s.size();
        string ans="";
        char ch;
        for(int i=n-1;i>=0;i--){
            if(s[i]!= ' ') ch = s[i]; 
            else ans =ch + ans;
        }
        return ch+ans;
    }
};