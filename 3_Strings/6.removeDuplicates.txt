


class Solution {
  public:
    string removeDuplicates(string& s) {
        int n=s.size();
        string ans="";

        ans=ans+s[0];

        for(int i=1;i<n;i++){
            if(s[i]!=s[i-1]){
                ans.push_back(s[i]);
            }
        }

        return ans;
    }
};