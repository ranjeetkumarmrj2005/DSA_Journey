

class Solution {
  public:
    string removeSpaces(string& s) {
        int n=s.size();
        string ans="";
        for(int i=0;i<n;i++){
            if(s[i]!=' ') ans=ans+s[i];
        }
        return ans;
    }
};