


class Solution {
public:
    int romanToInt(string s) {
        int n=s.size();
        unordered_map<char,int>m;
        m={
            {'I',1},
            {'V',5},
            {'X',10},
            {'L',50},
            {'C',100},
            {'D',500},
            {'M',1000}
        };
        int prev=0;
        int ans=0;
        for(int i=n-1;i>=0;i--){
            if(m[s[i]]>=prev){
                ans=ans+m[s[i]];
                prev=m[s[i]];
            }
            else{
                ans=ans-m[s[i]];
                prev=m[s[i]];
            }
        }
        return ans;
    }
};